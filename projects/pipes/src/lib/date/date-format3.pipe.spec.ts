import { DateFormat3Pipe } from './date-format3.pipe';

describe('DateFormat3Pipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormat3Pipe();
    expect(pipe).toBeTruthy();
  });
});
