import { TimeFormat2Pipe } from './time-format2.pipe';

describe('TimeFormat2Pipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormat2Pipe();
    expect(pipe).toBeTruthy();
  });
});
