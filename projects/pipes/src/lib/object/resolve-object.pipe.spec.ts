import { ResolveObjectPipe } from './resolve-object.pipe';

describe('ResolveObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
