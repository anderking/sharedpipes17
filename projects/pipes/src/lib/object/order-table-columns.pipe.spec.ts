import { OrderTableColumnsPipe } from './order-table-columns.pipe';

describe('OrderTableColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTableColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
